# Docker 镜像运行时依赖（宽松版本范围）。
# 与 pyproject.toml dependencies 对齐，去除 NiceGUI 死依赖与 pywin32（Linux 不需要）。
# 本地开发锁文件见 requirements.txt（uv compile，含 Windows 哈希，不可直接用于 Linux 镜像）。

# --- Web 后端：FastAPI + uvicorn ---
fastapi>=0.115.0
uvicorn>=0.30.0
starlette>=0.41.0
pydantic>=2.11.0
pydantic-core>=2.27.0
python-multipart>=0.0.20
jinja2>=3.1.5
aiofiles>=24.1.0
orjson>=3.10.0

# --- AI / HTTP ---
openai>=2.36.0
httpx>=0.28.0
requests>=2.31.0
urllib3>=2.0.0
certifi>=2024.12.14
charset-normalizer>=3.3.2
idna>=3.7
anyio>=4.8.0
sniffio>=1.3.1
h11>=0.14.0
httpcore>=1.0.7
google>=3.0.0
google-genai>=0.8.0

# --- 元数据 / 工具 ---
tmdbsimple>=2.9.1
structlog>=24.4.0
# diskcache：cache/metadata 的 SQLite 后端（碎 JSON+lock 文件迁移目标）
diskcache>=5.6.0
pyyaml>=6.0.2
python-dotenv>=1.0.1
typing-extensions>=4.12.0
annotated-types>=0.7.0
click>=8.1.0

# --- 媒体 / 压缩 / 字幕 ---
hachoir>=3.3.0
rarfile>=4.0
py7zr>=0.22.0
# ffsubsync（字幕自动对齐）默认 subtitle_sync_enabled=false，不装以省 numpy(70M)+ffsubsync。
# 需要字幕对齐时另行 pip install ffsubsync。
# scrapling：装 [fetchers] extra 拿全硬依赖（curl_cffi 非浏览器抓取核心 / playwright import 门槛 / browserforge 指纹），
# Dockerfile 装完删 patchright（反检测浏览器后端，138M 死重）+ playwright/driver（133M Node 浏览器驱动）。
# 浏览器抓取（DynamicFetcher）需额外 `playwright install` 下载浏览器二进制，默认 browser_enabled=false 不需要。
scrapling[fetchers]>=0.4.3
